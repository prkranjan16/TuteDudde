TASK-1

The program taking the integer from n and performing the factorial of the integer value. It is a recursive function, as the factorial function is being called inside the function itself and do (n-1). The result is then calculated and printed using the f-string operation.

TASK-2

The script importing math module to perform mathematical operations. The variable n is used to the integer value. The function math_operations is being used to perform squareroot, logarithm and sine operations. These values are returned and stored in square, log, and sine respectively. And then finally the output is printed.