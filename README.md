TASK-1:

Since the input function includes print function too, so instead of declaring the print function separately for input numbers
declaring the string in the input function to enter the numbers.
Once the numbers are entered, the arithmetic operations are done.

The script is performing Addition, Subtraction, Multiplication and division.

TASK-2:

Similar to TASK-1, the string sequence to enter the name is entered in the input function.
The values entered in the variables a & b is now joined and a sentence is created.